document.write(1+1)

let child
child = "철수"              // 변수에 값을 할당
console.log(child)

child = "영희"              // 변수에 값을 재할당
console.log(child)

const age = 13              // "13" 얘는 문자이므로 계산이 안됨, 값을 설정하는 것을 선언한다고 일컫는다. 
                            // 정해진 상수나 변수에 값을 넣는 것을 할당한다고 한다.
console.log(age)
// 13 + 2                   // 15
// "안녕" + "하세요"        // "안녕하세요", 문자는 두 개가 하나로 연결된다.
// "안녕" + "13"            // "안녕13"
// "안녕" + 13              // "안녕13" 어차피 계산이 안되므로 문자로 취급되어 문자로 연결이 된다.

// age = 8                  // 상수는 다른 값으로 변경이 불가능한다. 즉, 재할당이 되지 않는다.
// console.log(age)

const myMoney = 10000
console.log(myMoney)

